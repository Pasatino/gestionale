<%= form_with(model: tipo_prodotto) do |form| %>
  <% if tipo_prodotto.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tipo_prodotto.errors.count, "error") %> prohibited this tipo_prodotto from being saved:</h2>

      <ul>
        <% tipo_prodotto.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tipo, style: "display: block" %>
    <%= form.text_field :tipo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
